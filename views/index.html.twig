<html>
<head>
    <title>Doctor's Office List</title>
</head>
<body>
    <h1>Welcome to our office!</h1>

    <p>Create a new specialty:</p>

    <form action="/specialties" method="post">
        <label for="specialty_name">Specialty Name:</label>
        <input id="specialty_name" type="text" name="specialty_name">

        <button type="submit" name="button">Add Specialty</button>
    </form>

    {% if specialties is not empty %}
    <p>Here are our specialties:</p>
    <ul>
        {% for specialty in specialties %}
    </li><a href="/specialties/{{specialty.getID}}">{{specialty.getSpecialtyName}}</a></li>
        {% endfor %}
    </ul>
    {% else %}
        <p> There are no specialties yet! </p>
    {% endif %}

</body>
</html>
